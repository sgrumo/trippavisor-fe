import { component$ } from "@builder.io/qwik";
import { FormStore } from "@modular-forms/qwik";

interface SearchFormProps {
  form: FormStore<any, any>;
}

export const SearchForm = component$<SearchFormProps>(({ form }) => {
  return <></>;
});
